# ─── 필수 ───
streamlit==1.45.1      # 웹앱 프레임워크
Pillow==11.2.1         # PNG → base64 인코딩
openai==1.82.0         # GPT API (미사용이면 삭제)
python-dotenv==1.1.0   # .env 로컬용 (미사용이면 삭제)

# ─── 내부적으로 Streamlit이 자동 의존하지만, 버전 충돌 방지를 위해 명시 ───
altair==5.5.0
blinker==1.9.0
numpy==2.3.0
pandas==2.3.0
protobuf==5.29.5
requests==2.32.3
tenacity==9.1.2
tornado==6.5.1
